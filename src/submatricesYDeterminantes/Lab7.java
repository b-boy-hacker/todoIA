/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package submatricesYDeterminantes;

/**
 *
 * @author megus
 */
//El algoritmo para encontrar el determinante de una matriz de n x n,
//utiliza la estructura de código de llamada recursiva dentro de un ciclo:
public class Lab7 {
//1. Definir una representación de clase Matriz, con atributos de:
//elem[][], cantFil, cantCol, maxFil, maxCol, . . . Implementar 
//        constructor copia y otros métodos estándares para manipular una matriz.
//
//2. M1.toString() : Método standard que devuelve en cadena, los elementos de la matriz M1.
//
//3. M1.eliminarFil(k) : Método que elimina la fila k, de la matriz M1.
//        Se deben desplazar las filas posteriores a la fila k,para cubrir las filas eliminadas y decrementar la cantidad de filas.
//
//4. M1.eliminarCol(k):  Método que elimina la columna k, de la matriz M1.
//        Se deben desplazar las columnas posteriores a la columna k, para cubrir las columnas eliminadas y decrementar la cantidad de columnas.
//
//5. M1.generarRandom(a, b) : Método que genera n x m elementos 
//        aleatorios entre a y b, inclusive en la matriz M1.
//
//6. det(M1) : Función que devuelve la determinando de la matriz cuadrada M1, de elementos enteros.
//
//7. encontrarMenores(M1, L1) : Procedimiento que encuentra 
//        en la Lista de Matrices L1, todas las matrices menores derivados de la matriz M1.
//
//8. consulta(L1): Hacer algunas  consultas a la Lista de Matrices Menores, encontrados previamente.
    public static void main(String[] args) {
        
    }
}
